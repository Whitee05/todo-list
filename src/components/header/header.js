import React from "react";

import './header.sass';

const Header = () =>{
    return (
        <header className="main-header">
            <h1 className="main-header__title">Todo List</h1>
        </header>

            )
}

export default Header;